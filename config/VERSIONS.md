# Config version history

## 1_0_0

Initial release of the config library.

Public API: configure, merge, difference, missing, read, read_all, get,
no_omissions, save. Loads and merges JSON config files (defaults, overrides,
omissions) from a directory and saves diffs back.
