# JavaScript style

Not written yet. Add rules here when JS work appears in this repo.

Known owner preference already noted: the ternary `b ? a : c` reads clearly;
contrast with Python's conditional expression in `style/python.md`. Structure
the rules around what reads as one chunk, like the naming rules in
`style/common.md`.