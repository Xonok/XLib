# Re-Review: taskview/taskview.py

**File hash:** 643b17 (sha256sum, first 6 chars)  
**Date:** 2026-09-08  
**Reviewer:** Reviewer agent  
**Previous review:** `taskview-2026-09-08.md` (pre-fix)

---

## Verdict

**Approved.** All blocking issues resolved. Remaining items are minor style nits.

---

## Blocking Issues — Status

| # | Issue | Status | Notes |
|---|-------|--------|-------|
| 1 | Orchestrator not first | ✅ **Fixed** | `main()` now at line 28; pipeline map in module docstring (lines 6-8) |
| 2 | `compute_metrics` return undocumented | ✅ **Fixed** | Detailed docstring lines 110-122 with all keys and types |
| 3 | Narrow `inotify` exception catch | ✅ **Fixed** | Line 46: `except Exception:` — broad catch ensures polling fallback |

---

## Recommended Fixes — Status

| Issue | Status | Notes |
|-------|--------|-------|
| Header row not skipped | ✅ **Fixed** | Line 87-88: `if row[0] == "id": continue` |
| `cancelled` status ignored | ✅ **Fixed** | Lines 147-148: explicit handling, documented in `compute_metrics` logic |
| Unicode ellipsis | ✅ **Fixed** | Line 212: ASCII `"..."` |
| No signal handling | ✅ **Fixed** | Lines 288-289: SIGINT/SIGTERM handlers for clean `--watch` exit |
| Silent `OSError` in `read_csv` | ✅ **Fixed** | Line 107: logs to stderr |
| Magic number `80` | ⚠️ **Open** | Line 214: `width=80` — consider `shutil.get_terminal_size().columns` or constant |
| Internal functions not prefixed | ✅ **Fixed** | `_watch_inotify`, `_watch_poll` (lines 258, 273) |

---

## Remaining Nits (non-blocking)

| Issue | Location | Suggestion |
|-------|----------|------------|
| Negative sort key | Line 150 | `open_tasks.sort(key=lambda t: t["chg_ts"], reverse=True)` reads clearer |
| `width=80` magic number | Line 214 | `DEFAULT_WIDTH = 80` at top, or terminal detection |
| `_watch_poll` silent `OSError` | Line 283-284 | At least log on debug; same pattern as `read_csv` |
| `signal` import unused if not `--watch` | Line 11 | Minor; import is cheap |

---

## SPEC Compliance — Full ✅

All requirements from `taskview/SPEC.md` met:
- Data file, append-only, last-write-wins
- Comment/header skipping
- Render-once and `--watch` (1s poll, inotify with fallback)
- ANSI clear-screen
- Display layout: current / upcoming / pace / queue
- Current task = most recent `open` by `chg_ts`
- No API calls

---

## Style Compliance

| Guideline | Status |
|-----------|--------|
| `style/common.md` | ✅ Orchestrator first; returns documented; guards first; single pass |
| `style/python.md` | ✅ Tabs, comma imports, naming, type hints where needed, declarative where clear |
| `style/architecture.md` | ✅ Tool (not library); clean data flow |

---

## Approval

**This code is ready for merge/release.** The three blocking issues from the previous review are resolved. The remaining nits are cosmetic and do not affect correctness, SPEC compliance, or maintainability.

---

*Review keyed to file hash 643b17. If file changes, re-review needed.*